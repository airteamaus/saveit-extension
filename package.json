{
  "name": "saveit-extension",
  "version": "1.12.3",
  "description": "Save web pages and rediscover them with AI-powered metadata and search",
  "type": "module",
  "scripts": {
    "test": "node scripts/bundle-firebase.js && vitest run",
    "test:watch": "vitest",
    "test:coverage": "node scripts/bundle-firebase.js && vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:css:fix": "stylelint \"src/**/*.css\" --fix",
    "lint:all": "npm run lint && npm run lint:css",
    "format": "prettier --write \"src/**/*.css\"",
    "format:check": "prettier --check \"src/**/*.css\"",
    "build": "node scripts/bundle-firebase.js && web-ext build --overwrite-dest",
    "build:chrome": "node scripts/bundle-firebase.js && node scripts/build-chrome-package.js",
    "validate": "web-ext lint --ignore-files 'saveit-backend/**' --ignore-files 'scripts/**' --ignore-files 'coverage/**' --ignore-files 'tests/**' --ignore-files 'test-*.html' --ignore-files 'firefox-dev-profile/**'",
    "dev": "web-ext run",
    "upload-chrome": "node scripts/upload-chrome.js",
    "upload-chrome:publish": "node scripts/upload-chrome.js --publish",
    "upload-chrome:testers": "node scripts/upload-chrome.js --publish --target trustedTesters",
    "prepare": "husky"
  },
  "keywords": [
    "browser-extension",
    "firefox",
    "bookmarks",
    "ai",
    "search"
  ],
  "author": "Rich",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@playwright/test": "^1.57.0",
    "@vitest/coverage-v8": "^4.1.6",
    "@vitest/ui": "^4.1.6",
    "chokidar": "^4.0.3",
    "chrome-webstore-upload": "^4.0.3",
    "dotenv": "^17.2.3",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.4",
    "globals": "^16.5.0",
    "happy-dom": "^20.9.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.4",
    "stylelint": "^17.13.0",
    "stylelint-config-recommended": "^18.0.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-prettier": "^5.0.3",
    "vite": "7.3.2",
    "vitest": "^4.1.6",
    "web-ext": "^10.3.0",
    "webextension-polyfill": "^0.12.0",
    "zod": "^4.1.13"
  },
  "dependencies": {
    "@sentry/browser": "^8.55.0",
    "firebase": "^12.13.0"
  },
  "overrides": {
    "vite": "7.3.2"
  }
}
